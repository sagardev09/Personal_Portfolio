/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'cdn.hashnode.com', 'cdn.dribbble.com', 'private-user-images.githubusercontent.com', 'uploads-ssl.webflow.com', 'miro.medium.com', 'i.pinimg.com'],
    },
}
    ;

export default nextConfig;
